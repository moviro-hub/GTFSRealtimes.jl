# Generate Julia code from protobuf definition

This script donloads the latest protobuf definition and generates Julia code from it.

## Usage

```bash
julia generate/update_protobuf.jl
```

## Output

The output is a module written to the `src` directory.
